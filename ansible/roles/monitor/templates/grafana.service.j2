[Unit]
Description=Grafana Server
After=network-online.target
[Service]
User=prometheus
Group=prometheus
Type=simple
ExecStart={{ grafana_home }}/bin/grafana-server -homepath {{ grafana_home }}
Restart=always
RestartSec=1
[Install]
WantedBy=multi-user.target
